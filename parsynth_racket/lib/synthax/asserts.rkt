#lang racket


;; (define-syntax increasing-in-range
;;   (syntax-rules ()
;;     [(increasing-in-range [a ..] start end)
;;      (assert (and (incr-in [a ...] start end)))]))

;; (define-sequence-syntax incr-in )
